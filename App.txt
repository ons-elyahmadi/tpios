import SwiftUI
import Firebase

@main
struct YourApp: App {
    init() {
        FirebaseApp.configure() // Initialize Firebase
    }
    
    var body: some Scene {
        WindowGroup {
            SplashScreenView() // Start with Splash Screen
        }
    }
}
